[project]
name = "stuff-opener"
version = "0.1.0"
description = "Windows desktop workflow launcher for IT support teams."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pywebview==6.1",
]

[tool.uv]
package = false

[dependency-groups]
build = [
    "pyinstaller==6.21.0",
]
